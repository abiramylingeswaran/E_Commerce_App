import 'package:e_commerce_app/featurefile/auth/screens/onbordingpage.dart';
import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class OnBording extends StatelessWidget {
  OnBording({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Stack(
      children: [
        PageView(
          children: [
            // onBordingpage(image)
          ],
        )
      ],
    ));
  }
}
